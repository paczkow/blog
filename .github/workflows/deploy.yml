name: Build and Deploy to VPS

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/blog-paczkow-me

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Generate timestamp and git SHA tag
        id: vars
        run: |
          TIMESTAMP=$(date +'%Y-%m-%d')
          GIT_SHA=$(git rev-parse --short HEAD)
          FULL_TAG="${TIMESTAMP}-g${GIT_SHA}"
          echo "FULL_TAG=$FULL_TAG" >> $GITHUB_OUTPUT

      - name: Build and Push Docker Image with tags
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.FULL_TAG }}
      - name: Output deployment info
        run: |
          echo "✅ Deployment completed successfully!"
          echo "🐳 Pushed Docker images:"
          echo "   - ${{ env.IMAGE_NAME }}:latest"
          echo "   - ${{ env.IMAGE_NAME }}:${{ steps.vars.outputs.FULL_TAG }}"
          echo "🚀 VPS deployment triggered"
          echo "⏰ Watchtower will automatically update the running container"
